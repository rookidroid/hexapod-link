# Runtime dependencies for the app itself, browser or desktop.
#
#   requirements-desktop.txt  adds the native window and the build tooling
#   requirements-dev.txt      adds linters and the test runner

# Core Dash packages
dash>=4.4.1
dash-daq>=0.6.0
dash-bootstrap-components>=2.0.4

# Scientific computing
numpy>=1.24.0
plotly>=5.18.0

# Flask stack (Dash's underlying server)
Flask>=3.1.3
Flask-Compress>=1.24
Brotli>=1.2.0
click>=8.4.2
itsdangerous>=2.1.0
Jinja2>=3.1.6
MarkupSafe>=3.0.3
Werkzeug>=2.3.0
retrying>=1.4.2
